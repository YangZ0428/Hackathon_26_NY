# Add the Week planning tab

## Build
- Add a dedicated `/week` page using the existing ClimatePlan header and visual language.
- Show a static seven-day schedule with restrained forecast indicators and concise predicted-condition wording.
- Add three static ClimatePlan suggestion cards, three best forecast windows, and an expanded forecast-transparency panel.
- Make the existing Week navigation item open the new page while leaving Today, Me, and the planner unchanged.

## Technical details
- Use only local mock arrays and presentational React components; no APIs, persistence, location, weather, calendar, or AI logic.
- Use existing semantic colors and button components, and add unique Week page metadata.
- Verify desktop and mobile layouts, navigation, visible content, and browser errors.
